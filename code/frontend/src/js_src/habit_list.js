import { fetchData } from '/src/js_tools.js'
function habit_list(parent) {
	let apiUrl = localStorage.getItem('API_URL');
	let url = `${apiUrl}habit`
	
}

function habit_list_fetch_handle(fetch, parent) {
	return;
}